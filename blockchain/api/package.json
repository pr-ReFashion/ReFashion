{
  "name": "refashion-reward-api",
  "version": "1.0.0",
  "description": "Node.js API for ReFashion blockchain reward system",
  "main": "src/server.js",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "retry:failed": "node src/scripts/retryFailed.js",
    "retry:failed:dry": "node src/scripts/retryFailed.js --dry-run"
  },
  "dependencies": {
    "dotenv": "^16.4.0",
    "ethers": "^6.10.0",
    "express": "^4.18.0",
    "pg": "^8.11.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
